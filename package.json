{
	"name": "forja-fields",
	"version": "0.1.0",
	"description": "Campos personalizados definidos por código para WordPress.",
	"license": "GPL-2.0-or-later",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"watch": "vite build --watch",
		"typecheck": "tsc --noEmit",
		"test:e2e": "playwright test",
		"test:e2e:ui": "playwright test --ui"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/bun": "^1.3.0",
		"typescript": "^5.9.0",
		"vite": "^7.1.0"
	}
}
